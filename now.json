{
    "version": 2,
    "name": "patient-payment",
    "builds": [
        { "src": "./backend/*.js", "use": "@now/node" }
    ],
    "routes": [
        { "src": "/api/(.*)", "dest": "/backend/$1" }
    ]
}
